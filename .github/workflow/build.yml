name: Build AntiDropshot Plugin

on: [push]

jobs:
  build:
    runs-on: windows-2022
    steps:
    - uses: actions/checkout@v4

    - name: Setup NuGet
      uses: NuGet/setup-nuget@v1

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Restore NuGet Packages
      # Здесь должно быть имя вашего актуального .csproj файла
      run: nuget restore AntiDropshot.csproj

    - name: Build
      # Указываем правильный файл проекта для сборки
      run: msbuild AntiDropshot.csproj /p:Configuration=Release /p:TargetFramework=net48

    - name: Upload DLL
      uses: actions/upload-artifact@v4
      with:
        name: AntiDropshot-Release
        path: bin/Release/net48/AntiDropshot.dll
